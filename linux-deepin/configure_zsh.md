### configure and install 
```
$ sudo apt-get install -y zsh
$ vim ~/.zshrc
```
> 添加 [skywind3000](https://github.com/skywind3000/vim/blob/master/etc/zshrc.zsh) 配置


### make zsh as default shell
```
$ chsh -s $(which zsh)
```
